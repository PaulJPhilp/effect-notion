// Types local to the NotionClient service.
// Currently NotionClient relies on schemas from src/NotionSchema.ts, so we
// don't define additional local types here. This file exists to keep the
// contract consistent and is a home for future client-specific types.
export {};
