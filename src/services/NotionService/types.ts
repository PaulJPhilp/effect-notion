// Types local to the NotionService.
// Currently the service uses shared types from src/NotionSchema.ts and
// does not need additional local types. This file exists to keep the
// contract consistent and is a home for future service-specific types.
export {};
